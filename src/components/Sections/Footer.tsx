import { FC } from "react";

interface FooterProps {}

const Footer: FC<FooterProps> = ({}) => {
  return <footer className="mt-12 flex flex-col items-center">Footer</footer>;
};

export default Footer;
