import { FC } from "react";

interface FeaturesProps {}

const Features: FC<FeaturesProps> = ({}) => {
  return <div className="mt-12 flex flex-col items-center">Features</div>;
};

export default Features;
